

## 文档对照检查

已阅读 degrade_strategy2.docx 和 cubesandbox.docx：

### 关键架构要素
1. **六层分层模型**: 用户访问层 → API网关层 → 编排调度层 → 运行时管理层 → 虚拟化抽象层 → 基础设施层
2. **三运行时**: CubeSandbox (<60ms) + Kata (60-150ms) + Incus (秒级)
3. **Cloud Hypervisor**: 主后端，全栈 Rust 实现
4. **管理平面**: Web UI + CLI，约15,000行代码，代码重度4.1
5. **双模产品**: Agent模式(AI SDK兼容) + 传统模式(PVE替代)

### 当前 Rust 结构对照
| 文档组件 | 当前 crate | 状态 |
|----------|-----------|------|
| API 网关层 | api-gateway | ✅ Axum stub |
| CLI 工具 | cubeconsole | ✅ clap stub |
| 管理平面 Web UI | web/ | ✅ React stub |
| 运行时管理 | cubesandbox-operator | ⚠️ stub |
| 编排调度 | instance-controller | ⚠️ stub (K8s) |
| Cloud Hypervisor | - | ❌ 未创建 |
| 版本工具 | version-tool | ✅ stub |

### 差距
1. 缺少 Cloud Hypervisor crate（文档明确主后端，全栈 Rust）
2. 缺少统一编排层（Instance CRD/HA）
3. 前端仅为空壳，需要对接 API

## 下一步
需要补充：
- crates/cloud-hypervisor/ - Cloud Hypervisor 主后端集成
- crates/orchestrator/ 或统一编排层
- web/ 前端实现（Instances, Storage, Networks 视图）
