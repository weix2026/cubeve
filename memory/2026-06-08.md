14/14 L0 API 测试全部通过！
- 修复 `createInstance` 处理程序：使用完整请求体（`map[string]interface{}`）而非仅 `req.Config`
- 修复 `createInstance` 以支持正确的 Incus API 格式（`name`、`source`、`type`）
- 所有测试通过：health、ready、instances、storage-pools、networks、profiles